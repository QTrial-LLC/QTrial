-- Seed-data down-migration: intentionally empty.
--
-- Reverting reference-data seeds is not a DELETE operation. Restore
-- from backup if a true rollback is needed. This file exists so the
-- migration directory stays in reversible mode; it is a deliberate
-- no-op.

SELECT 1;
